import { ListNode } from "../base/ListNode";
import { findFromEnd } from "../findFromEnd"

/**
 * 移除链表的倒数第 K 个节点 (0 < K <= N)
 * @param list 
 * @param k 
 */
function removeNthFromEnd<T>(list: ListNode<T>, k: number) {
  
}